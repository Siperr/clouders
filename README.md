# Clouders

Lightweight google drive clone built with Express and EJS (SSR).

# TODO 
- [ ] add shared folder feature
    - update user permission feature
    - delete shared folder
    - rename shared folder
    - view users who share the folder
    - notification centre to accept an invite to share a folder?
- [ ] error handling: add see more button to see the precise error
